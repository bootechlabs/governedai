// Shared Tailwind class tokens for the small set of form controls reused
// across sign-in, the inventory add-row, and the system detail page's
// forms — kept in one place so the look stays consistent as new forms
// are added instead of each page retyping the same utility string.
export const inputClass =
  "rounded border border-zinc-300 bg-transparent px-2 py-1.5 text-sm dark:border-zinc-700 dark:bg-zinc-900";

export const primaryButtonClass =
  "rounded bg-black px-3 py-1.5 text-sm font-medium text-white dark:bg-white dark:text-black";

export const subtleLinkClass = "text-zinc-500 underline hover:no-underline";
